---
interface Props {
    placeholder: string;
    type: "text" | "email" | "password" | "number";
    Text: string;
    id: string; 
}

const { placeholder, type, Text, id } = Astro.props;
---

<div class="space-y-2 w-full">
    <label for={id} class="text-tremor-default font-medium text-tremor-content-strong dark:text-dark-tremor-content-strong">{Text}</label>
    <input 
        id={id} 
        type={type} 
        placeholder={placeholder} 
        required 
        class="bg-[#F5F6FA] w-full min-h-6 mt-3 rounded-[4px] border border-[#D5D5D5] px-[20px] py-[7px] mb-2 text-tremor-default font-medium text-tremor-content-strong dark:text-dark-tremor-content-strong transition-transform transform outline-none focus:translate-y-[-5px]">

    </div>