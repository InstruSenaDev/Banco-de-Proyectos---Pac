import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173, // Asegúrate de que este puerto coincida con el puerto en el que está ejecutando tu frontend
  },
})